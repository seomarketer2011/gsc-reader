# gsc-ingestor

Search Console import job (Phase 3+). Pulls daily Page+Query+Device+Country rows per property and upserts them idempotently into BigQuery. See docs/ARCHITECTURE.md.

Placeholder — implemented in the phase noted above (docs/IMPLEMENTATION_PLAN.md).
